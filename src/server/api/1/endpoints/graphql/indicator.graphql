type Query {
    indicators: [Indicator],
    indicator(id: String!): Indicator
}

type Indicator {
    id: String,
    name: String,
    topics: JSON,
    sourceOrganization: String,
    sourceNote: String,
    source: JSON,
    unit: String
}

